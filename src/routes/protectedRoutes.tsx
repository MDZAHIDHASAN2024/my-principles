// import { Navigate } from 'react-router';

// type ProType = {
//   isLoggedIn: boolean;
//   children: React.ReactNode;
// };

// const ProtectedRoutes = ({ isLoggedIn, children }: ProType) => {
//   if (!isLoggedIn) {
//     return <Navigate to="/" replace />;
//   }
//   return children;
// };

// export default ProtectedRoutes;
